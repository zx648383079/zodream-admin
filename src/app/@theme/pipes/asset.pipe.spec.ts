/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AssetPipe } from './asset.pipe';

describe('Pipe: Assete', () => {
  it('create an instance', () => {
    let pipe = new AssetPipe();
    expect(pipe).toBeTruthy();
  });
});
