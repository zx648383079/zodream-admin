/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CutPipe } from './cut.pipe';

describe('Pipe: Cute', () => {
  it('create an instance', () => {
    let pipe = new CutPipe();
    expect(pipe).toBeTruthy();
  });
});
